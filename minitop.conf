# minitop configuration file
# Lines starting with '#' are comments.

# Refresh delay in seconds (float)
delay = 1.0

# Sort column (cpu or mem)
sort = cpu

# Operating mode (monitor or scheduler)
mode = monitor
